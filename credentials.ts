import Credentials from './credentials.template'

const credentials: Credentials = {
    host: 'localhost',
    user: 'nest-app',
    password: 'RNmkXERn',
    database: 'nest_app'
}
export default credentials;
